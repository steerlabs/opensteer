# Changelog

## Unreleased

- Breaking: removed legacy `ai` config from `OversteerConfig`; use top-level `model` instead.
- Breaking: `OVERSTEER_AI_MODEL` is no longer supported; use `OVERSTEER_MODEL`.
- Oversteer now enables built-in LLM resolve/extract by default with model `gpt-5.1`.

## 0.1.0

- Initial open-source release.
