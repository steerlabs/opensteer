# Changesets

Use `pnpm changeset` for any pull request that changes published behavior, package
metadata, or user-facing docs that should ship in a release.

Use `pnpm changeset --empty` for repository-only changes such as CI, build
tooling, docs-only maintenance, or release-process updates that should not bump
package versions.
